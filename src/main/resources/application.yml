spring:
  profiles:
    active: local
  datasource:
    url: jdbc:mysql://localhost:3306/hello_bot?serverTimezone=Asia/Seoul
    username: root
    password: sa

  jpa:
    show-sql: true
    hibernate:
      naming:
        implicit-strategy: org.hibernate.boot.model.naming.ImplicitNamingStrategyLegacyJpaImpl
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
      ddl-auto: validate
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    properties:
      hibernate.default_batch_fetch_size: 1000
  flyway:
    url: jdbc:mysql://localhost:3306/hello_bot?serverTimezone=UTC
    schemas: hello_bot
    user: root
    password: sa
    enabled: true
    clean-on-validation-error: true

---
spring:
  profiles: test

  datasource:
    url: jdbc:mysql://localhost:3306/test_laboratory_persistence?serverTimezone=UTC
    username: root
    password: sa

  jpa:
    show-sql: true
    hibernate:
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
      ddl-auto: validate
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect

---
spring:
  profiles: production

  datasource:
    url: jdbc:mysql://hanteam-test.cefqekotw3q5.ap-northeast-2.rds.amazonaws.com:3306/test_laboratory_persistence?serverTimezone=UTC
    username: root
    password: sa

  jpa:
    show-sql: true
    hibernate:
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
      ddl-auto: validate
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect

external:
  purpleMemberInfo:
    requestUri: https://dev.ppuple.com/api/member/login/session